[pytest]
testpaths = tests
python_files = test_*.py
# Quieter default — uvicorn deprecation chatter from imported modules is
# not a test concern. Override with `pytest -v` for the noisy view.
addopts = -q
filterwarnings =
    ignore::DeprecationWarning:fastapi.*
