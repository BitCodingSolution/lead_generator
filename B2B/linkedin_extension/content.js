// LinkedIn messaging content script — Phase 1 stub.
// Phase 5 will add reply-thread capture + warning-phrase detection.

(() => {
  console.log("[BitCoding LinkedIn] messaging content.js loaded (Phase 1 stub)");
})();
