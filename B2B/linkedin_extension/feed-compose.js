// LinkedIn feed composer aid — Phase 1 stub.
// Phase 6 will add the post-draft generator that pastes into the composer.

(() => {
  console.log("[BitCoding LinkedIn] feed-compose loaded (Phase 1 stub)");
})();
